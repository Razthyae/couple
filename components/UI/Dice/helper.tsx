
/* 
export const calculateNewTile = (roll: number, currentTile: number, stateForHelper: object) => {


let result: number = currentTile + roll
if(result > 22) {
    result -= 22
    let newRound = round
    newRound[active] += 1
    console.log(newRound)
    setTimeout(() => setRound(newRound), 500)
}
return result
}

 */